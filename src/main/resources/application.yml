app:
  short-host: https://f200.co/
micronaut:
  server:
    port: 8082
  application:
    name: urlshortner
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
      swagger-ui:
        paths: classpath:META-INF/swagger/views/swagger-ui
        mapping: /swagger-ui/**
redis:
  # ideally these should be in env var instead of pasted here!
  uri: redis-12545.c293.eu-central-1-1.ec2.cloud.redislabs.com:12545
  password: a0ghKktOLx6cupw42q3UbGsHUbvWFr2c
  expiry: 2628288 # expiry of record in seconds (~30days if url is not expanded)
netty:
  default:
    allocator:
      max-order: 3
